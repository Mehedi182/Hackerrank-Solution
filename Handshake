#include <bits/stdc++.h>

using namespace std;


int main()
{

    long long int t,n,i;
    cin>>t;
    while(t--)
    {
       long long  int s1=1,s2=1;
        cin>>n;
        n=n*(n-1);
        n=n/2;
        cout<<n<<endl;
    }
}
