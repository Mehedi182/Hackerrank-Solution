#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    int l,n,i,c=0;
    cin>>n;
    while(n--){
    cin>>s;
    l=s.size();
    for(i=0;i<l;i++)
    {
        if(s[i]==s[i+1])
        {
            c++;
        }
    }
    cout<<c<<endl;
    c=0;
}
}
